VITE_API_URL=http://localhost:8000

# Cloudinary (unsigned upload) — needed for the "upload from device" button
# on the add/edit product page. Without these, the plain "paste an image
# URL" field still works fine on its own.
VITE_CLOUDINARY_CLOUD_NAME=
VITE_CLOUDINARY_UPLOAD_PRESET=
